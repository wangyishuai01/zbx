<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.cn.zbx.dao.TWatchRecordDao">
	<resultMap id="BaseResultMap" type="com.cn.zbx.pojo.tWatchRecord">
		<id column="record_id" property="recordId" jdbcType="BIGINT" />
		<result column="user_id" property="userId" jdbcType="BIGINT" />
		<result column="res_id" property="resId" jdbcType="BIGINT" />
		<result column="create_time" property="createTime" jdbcType="DATE" />
	</resultMap>
	
	<sql id="Base_Column_List"> record_id, user_id, res_id </sql>

	<insert id="insert" parameterType="com.cn.zbx.pojo.tWatchRecord">
		insert into t_watch_record (user_id, res_id, create_time)
		values (#{userId,jdbcType=BIGINT}, #{resId,jdbcType=BIGINT}, #{createTime,jdbcType=DATE})
	</insert>

	<update id="update" parameterType="com.cn.zbx.pojo.TCategory">
		update t_watch_record
		<set>
			<if test="userId != null">
				user_id = #{userId,jdbcType=BIGINT},
			</if>
			<if test="resId != null">
				res_id = #{resId,jdbcType=BIGINT},
			</if>
		</set>
		where record_id = #{recordId,jdbcType=BIGINT}
	</update>

	<!-- 返回多条数据 -->
	<select id="select" resultMap="BaseResultMap" parameterType="com.cn.zbx.pojo.TCategory">
		select <include refid="Base_Column_List" /> from t_watch_record
		<where>
			<if test="recordId != null">
				and record_id = #{recordId,jdbcType=BIGINT}
			</if>
			<if test="userId != null">
				and user_id = #{userId,jdbcType=BIGINT}
			</if>
			<if test="resId != null">
				and res_id = #{resId,jdbcType=BIGINT}
			</if>
		</where>
	</select>

	<!-- 返回单条数据 -->
	<select id="selectByPojo" resultMap="BaseResultMap" parameterType="com.cn.zbx.pojo.TCategory">
		select <include refid="Base_Column_List" /> from t_watch_record
		<where>
			<if test="recordId != null">
				and record_id = #{recordId,jdbcType=BIGINT}
			</if>
			<if test="userId != null">
				and user_id = #{userId,jdbcType=BIGINT}
			</if>
			<if test="resId != null">
				and res_id = #{resId,jdbcType=BIGINT}
			</if>
		</where>
	</select>
	
	<!-- 返回单条数据 -->
	<select id="selectCountByPojo" resultType="java.lang.Integer" parameterType="com.cn.zbx.pojo.TCategory">
		select Count(record_id) from t_watch_record
		<where>
			<if test="recordId != null">
				and record_id = #{recordId,jdbcType=BIGINT}
			</if>
			<if test="userId != null">
				and user_id = #{userId,jdbcType=BIGINT}
			</if>
			<if test="resId != null">
				and res_id = #{resId,jdbcType=BIGINT}
			</if>
		</where>
	</select>

</mapper>